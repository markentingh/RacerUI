<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - RacerUI</title>
    <link rel="stylesheet" href="/css/app.css"/>
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans:ital,wdth,wght@0,87.5,100..900;1,87.5,100..900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@24,400,0,0" />
    @await RenderSectionAsync("Head", required: false)
</head>
<body>
    <div class="init"></div>
    <div class="dash">
        <nav>
            <div class="logo">
                <svg viewBox="0 0 1850 705" class="light">
                    <use xlink:href="#racerui-logo" x="0" y="0" width="1850" height="705" class="svg-nocolor"></use>
                </svg>
                <svg viewBox="0 0 1850 705" class="dark">
                    <use xlink:href="#racerui-logo-light" x="0" y="0" width="1850" height="705" class="svg-nocolor"></use>
                </svg>
            </div>
            <div class="arrow">
                <svg viewBox="0 0 64 64" class="dark">
                    <use xlink:href="#icon-arrow-right" x="0" y="0" width="64" height="64"></use>
                </svg>
            </div>
            <ul class="menu">
                <li class="item-game" onclick="RacerUI.nav.navigate('game')">
                    <svg viewBox="0 0 657 657" class="dark">
                        <use xlink:href="#icon-game" x="0" y="0" width="657" height="657"></use>
                    </svg>
                    <span id="gameName">Game</span>
                </li>
                <li class="item-cars" onclick="RacerUI.nav.navigate('cars')">
                    <svg viewBox="0 0 1850 705" class="dark">
                        <use xlink:href="#icon-race-car" x="0" y="0" width="1850" height="705"></use>
                    </svg>
                    <span>Cars</span>
                </li>
                <li class="item-tracks" onclick="RacerUI.nav.navigate('tracks')">
                    <svg viewBox="0 0 800 657" class="dark">
                        <use xlink:href="#icon-track" x="0" y="0" width="800" height="657"></use>
                    </svg>
                    <span>Tracks</span>
                </li>
                <li class="item-history" onclick="RacerUI.nav.navigate('history')">
                    <svg viewBox="0 0 800 657" class="dark">
                        <use xlink:href="#icon-history" x="0" y="0" width="800" height="657"></use>
                    </svg>
                    <span>History</span>
                </li>
            </ul>
            <darkmode-toggle></darkmode-toggle>
            <div class="dropdown-menu" style="display:none;">
                <div class="hamburger">
                    <span class="material-symbols-outlined">menu</span>
                </div>
                <ul>
                </ul>
            </div>
        </nav>
        @RenderBody()
    </div>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
